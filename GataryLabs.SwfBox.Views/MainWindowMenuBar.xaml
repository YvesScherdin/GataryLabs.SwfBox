<views:MvvmUserControl x:Class="GataryLabs.SwfBox.Views.MainWindowMenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:GataryLabs.SwfBox.Views"
             xmlns:viewModels="clr-namespace:GataryLabs.SwfBox.ViewModels.Abstractions;assembly=GataryLabs.SwfBox.ViewModels.Abstractions"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModels:IMainWindowMenuBarViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <DockPanel>

            <Grid DockPanel.Dock="Top">
                    
                <StackPanel>
                    <!--
                    <Image Source="pack://application:,,,/GataryLabs.SwfBox.Views;component/Resources/Icon_SwfFileDefault_128x128.png"/>
                    -->
                    <Label Content="SWF Box"
                            Style="{StaticResource Main.MenuBar.Headline.Big}"/>
                        
                    <Button Content="Overview"
                            Command="{Binding OpenOverviewCommand}"
                            Style="{StaticResource Main.MenuBar.ActionButton.Big}"/>
                        
                    <Button Content="Settings"
                            Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource Main.MenuBar.ActionButton.Big}"/>
                        
                </StackPanel>
                    
            </Grid>

            <Grid DockPanel.Dock="Top" Margin="0 20 0 6">
                <Separator Background="White"/>
            </Grid>

            <Grid DockPanel.Dock="Top">
                    
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>   
                    
                <Label Content="Recent SWFs"
                       Style="{StaticResource Main.MenuBar.Headline.Big}"
                       Grid.Row="0"/>

                <ListBox ItemsSource="{Binding RecentSwfFiles.Files}"
                         ScrollViewer.HorizontalScrollBarVisibility="Visible"
                         ScrollViewer.VerticalScrollBarVisibility="Visible"
                         Style="{StaticResource Main.MenuBar.ListBox}"
                         ItemContainerStyle="{StaticResource Main.MenuBar.ItemContainer}"
                         Grid.Row="1">
                        
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid ToolTip="{Binding Path}">
                                <Border Padding="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Source="{Binding Image}"
                                                Grid.Column="0" Width="24" Height="24"/>
                                        <Label Content="{Binding Title}"
                                                Grid.Column="1"
                                                Style="{StaticResource Main.MenuBar.ListItem.Text}" />
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                        
                </ListBox>
                    
            </Grid>
  
        </DockPanel>
            
    </Grid>
    
</views:MvvmUserControl>
