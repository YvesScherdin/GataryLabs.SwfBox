<mvvmViews:MvvmWindow x:Class="GataryLabs.SwfBox.Views.MainWindow"
        xmlns:views="clr-namespace:GataryLabs.SwfBox.Views"
        xmlns:mvvmViews="clr-namespace:GataryLabs.Mvvm.Views;assembly=GataryLabs.Mvvm.Views"
        xmlns:viewModels="clr-namespace:GataryLabs.SwfBox.ViewModels.Abstractions;assembly=GataryLabs.SwfBox.ViewModels.Abstractions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="clr-namespace:Notification.Wpf.Controls;assembly=Notification.Wpf"
        mc:Ignorable="d"
        TitleVisibility="Collapsed"
        IconVisibility="Collapsed"
        Height="450" Width="800"
        MinHeight="420" MinWidth="640"
        d:DataContext="{d:DesignInstance viewModels:IMainWindowViewModel, IsDesignTimeCreatable=False}">

    <mvvmViews:MvvmWindow.Style>
        <Style TargetType="Window" BasedOn="{StaticResource ResourceKey={x:Type Window}}"/>
    </mvvmViews:MvvmWindow.Style>

    <Grid>
        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Left" Width="200">

                <Border Style="{StaticResource Main.MenuBar.Border}">

                    <views:MainWindowMenuBar DataContext="{Binding MenuBarViewModel}"/>

                </Border>

            </Grid>

            <Grid DockPanel.Dock="Right">

                <Grid>
                    <controls:NotificationArea x:Name="ContentNotificationArea"
                                               Position="TopCenter"
                                               MaxItems="1"
                                               Panel.ZIndex="1000">

                    </controls:NotificationArea>

                    <Border Style="{StaticResource Main.MainRegion.Border}">

                        <ScrollViewer>

                            <ContentControl Content="{Binding ContentNavigator.ContentViewModel}"
                                            ContentTemplateSelector="{StaticResource DataTemplateSelector.ViewModels}"/>

                        </ScrollViewer>

                    </Border>
                    
                </Grid>
                
            </Grid>
        </DockPanel>
    </Grid>
</mvvmViews:MvvmWindow>
